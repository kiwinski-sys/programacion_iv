import 'package:flutter/material.dart';

class GeneroScreen extends StatelessWidget {
   
const GeneroScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
         child: Text('GeneroScreen'),
      ),
    );
  }
}